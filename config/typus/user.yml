# Use the README file as a reference to customize settings.

User:
  fields:
    default: name, email, encrypted_password, salt
    form: name, email, encrypted_password, salt
  relationships: 
  application: Application
